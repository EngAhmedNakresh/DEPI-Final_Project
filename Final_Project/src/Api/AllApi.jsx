/**
 * Data provider wrapper. Replace the empty implementation with your API when ready.
 * Usage: wrap the app in <AllData> and add shared API state here when ready.
 */
function AllData({ children }) {
  return children;
}

export { AllData };
